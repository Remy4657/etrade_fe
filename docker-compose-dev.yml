services:
  frontend:
    image: etrade-frontend
    networks:
      - etrade-network
    env_file:
      - .env
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev
networks:
  etrade-network:
    external: true
