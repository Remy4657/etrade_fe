services:
  frontend:
    image: remy4657/test-etrade-fe-ci:latest
    container_name: etrade_frontend
    restart: always
    networks:
      - etrade-network
    env_file:
      - .env
    ports:
      - "3000:3000"
networks:
  etrade-network:
    external: true
